{
  "name": "newg-inworld-tts-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Holds the Inworld API key server-side and proxies TTS requests for the NewG site.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
